# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase on push
'on': 
  push:
    branches:
      - feature/create-nft-minted
jobs:
  build_and_preview:
    if: '${{ github.event.pull_request.head.repo.full_name == github.repository }}'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          npm i
      - name: Replace Action
        uses: datamonsters/replace-action@v2
        with:
          files: '/home/runner/work/IsengardMarket/IsengardMarket/node_modules/@elrondnetwork/erdjs/out/smartcontracts/wrapper/systemWrapper.js'
          replacements: '__dirname="."'
      - run: |
          npm i @angular/cli
          npm run-script build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_ISENGARD_20253 }}'
          projectId: isengard-20253
